"""oMLX shared kernel helpers (``custom_kernels/common/csrc``).

Only the native-build headers live here; there is no Python code. The
package marker exists so setuptools package-data ships the ``csrc`` tree.
"""
